/**
 * The arrow function (=>) is a shorthand syntax for an anonymous function.
 * param => statements or expression
 */

var ovenTen = x => x>10 ? 10 : x;
console.log(ovenTen(8));
console.log(ovenTen(12));